---
interface ModalHeaderProps {
	title: string;
}
const { title } = Astro.props as ModalHeaderProps;
---

<p class='font-cormorant text-[28px] md:text-[40px] text-lightblack text-center'>{title}</p>
